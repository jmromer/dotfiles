# ~/.tmuxinator/blog.yml

name: blog
root: ~/Developer/blog

windows:
  - blog:
      layout: split
      panes:
        - ghost:
          - n 0.10.38 && npm start
        - buster:
          - which python && python --version && pip install --upgrade buster
