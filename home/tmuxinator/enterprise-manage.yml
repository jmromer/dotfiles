# ~/.tmuxinator/enterprise-manage.yml

name: enterprise-manage
root: ~/github/enterprise2/enterprise-manage

windows:
  - editor: emacsclient -nw
  - shell:
  - repl: script/console --pry
  - server: export PATH="${PATH/\/usr\/local\/opt\/coreutils\/libexec\/gnubin://}" && script/server
  - dotfiles:
      root: ~/.dotfiles
      panes:
        -
