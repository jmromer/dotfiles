# ~/.tmuxinator/github.yml

name: github
root: ~/github/github

windows:
  - editor: e +OpenSession
  - shell:
  - repl: script/console --pry
  - server: script/server
  - dotfiles:
      root: ~/.dotfiles
      panes:
        -
