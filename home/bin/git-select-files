#!/usr/bin/env bash

# fuzzy-select files listed via `git status`
#
# Usage:
#   git select-files [FZF_OPTS]

git status -s |
    fzf --multi --tiebreak=index $1 --preview='less {+2} 2>/dev/null' |
    sed 's/^[^\s]*\s//'
