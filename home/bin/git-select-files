#!/usr/bin/env bash

# fuzzy-select files listed via `git status`
#
# Usage:
#   git select-files [FZF_OPTS]

# fuzzy-select files
files="$(git status -s |\
  sed 's/^[^\s]*\s//' |\
  fzf --ansi --no-sort --reverse --tiebreak=index $1)"

if [[ ! -z $files ]]; then
  echo "$files"
fi
