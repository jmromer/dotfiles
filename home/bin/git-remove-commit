#!/usr/bin/env bash

# Removes a commit from your git repo's history

if [[ -z "$1" ]]; then
  sha="$(git select-sha)"
else
  sha="$1"
fi

echo "git rebase --onto $sha^ $sha"
git rebase --onto "$sha"^ "$sha"
