#!/usr/bin/env bash

set -e

DOTFILE_PATH="$HOME/.dotfiles/home"

target_filename="$1"
target_file="$DOTFILE_PATH/$target_filename"
link_to_file="$HOME/.$target_filename"

touch "$target_file"

ln -s "$target_file" "$link_to_file"

vim "$link_to_file"
