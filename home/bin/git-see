#!/usr/bin/env bash

# `git show` a fuzzy-selected commit
#
# Usage:
#   git see

# fuzzy-select a SHA
commit_sha="$(git-select-sha)"

if [[ ! -z $commit_sha ]]; then
  git show --pretty=fuller "$commit_sha"
fi
