#!/usr/bin/env bash

set -e

controller_name="$1"

git pull origin master

git bc "org-idm/$controller_name"

org-sso-scaffold "$controller_name"

git add --all
git commit --no-verify -m "WIP: Enforce SAML SSO on $controller_name [ci skip]"
git push

org-sso-generate-pr "$controller_name"

hub pr

