#!/bin/bash

# Fuzzy-select a homebrew package to install
#
# Usage:
#   brew get
#

set -e

# select from packages available to download
package="$(brew select search)"

if [[ -z "$package" ]]; then
  echo "No package selected."
  exit 1
fi

echo "brew $1 install $package"
brew $1 install $package
