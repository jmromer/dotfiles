#!/bin/bash

args=()
for arg in "$@"; do
  if [[ "$arg" == "--languages=Ruby" ]]; then
    args+=("--languages=+Ruby" "--exclude=*.toml")
  else
    args+=("$arg")
  fi
done

"${HOMEBREW_PREFIX}/bin/ctags" "${args[@]}"
